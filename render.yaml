services:
  - type: web
    name: vishesh-portfolio
    env: static
    buildCommand: npm ci && chmod -R +x node_modules/.bin && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html